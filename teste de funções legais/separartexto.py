def separar_texto(texto, separador):
    palavras = texto.split(separador)
    return palavras
# Exemplo de uso
# texto = input('Digite um texto: ')
# separador = input('Digite o separador: ')
# resultado = separar_texto(texto, separador)
# print(f'Palavras separadas: {resultado}')
